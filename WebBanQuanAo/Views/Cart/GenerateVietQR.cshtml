@{
    ViewData["Title"] = "Thanh toán qua VietQR";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm border-0">
                <div class="card-body p-4 text-center">
                    <h2 class="mb-4" style="color: var(--secondary-color); font-weight: 600;">Thanh toán đơn hàng</h2>

                    <div class="mb-4">
                        <p class="text-muted">Quét mã QR bằng ứng dụng ngân hàng để hoàn tất thanh toán</p>
                    </div>

                    @if (!string.IsNullOrEmpty(ViewBag.QRCodeImage))
                    {
                        <div class="qr-container mb-4 p-3" style="background-color: #fff; border-radius: 8px; border: 2px dashed var(--primary-color); display: inline-block;">
                            <img src="@ViewBag.QRCodeImage" alt="QR Code Thanh toán" style="width: 300px; height: 300px;" />
                        </div>

                        <div class="payment-details mx-auto mb-4 text-start" style="max-width: 400px; background-color: #f8f9fa; padding: 15px; border-radius: 8px;">
                            <p><strong>Ngân hàng:</strong> BIDV</p>
                            <p><strong>Số tài khoản:</strong> 6411112098</p>
                            <p><strong>Chủ tài khoản:</strong>DOAN NGOC KHANH LY</p>
                            <p><strong>Nội dung chuyển khoản:</strong> Như trong mã QR</p>
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-danger">
                            <i class="fas fa-exclamation-circle me-2"></i> Không thể tạo mã QR. Vui lòng thử lại sau.
                        </div>
                    }

                    <div class="mt-4">
                        <a asp-action="Index" class="btn btn-outline-custom me-2">
                            <i class="fas fa-arrow-left me-2"></i>Quay lại giỏ hàng
                        </a>
                        <a href="/" class="btn btn-custom">
                            <i class="fas fa-home me-2"></i>Về trang chủ
                        </a>
                    </div>

                    <div class="mt-4 text-muted small">
                        <p>Sau khi thanh toán thành công, đơn hàng của bạn sẽ được xử lý ngay lập tức.</p>
                        <p>Mọi thắc mắc xin liên hệ hỗ trợ trực tuyến hoặc hotline: <strong style="color: var(--primary-color);">0395752407</strong></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>